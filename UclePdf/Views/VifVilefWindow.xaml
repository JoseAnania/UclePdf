<Window x:Class="UclePdf.Views.VifVilefWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:UclePdf.ViewModels"
        Title="VIF/VILeF (IC)" Height="430" Width="780">
    <Window.Resources>
        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="Margin" Value="0,0,8,0"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Background" Value="#FF1976D2"/>
            <Setter Property="Foreground" Value="White"/>
        </Style>
        <Style x:Key="DangerButton" TargetType="Button" BasedOn="{StaticResource PrimaryButton}">
            <Setter Property="Background" Value="#FFD32F2F"/>
        </Style>
        <Style x:Key="HeaderCell" TargetType="Border">
            <Setter Property="Background" Value="#FFEFEFEF"/>
            <Setter Property="Padding" Value="6"/>
            <Setter Property="BorderBrush" Value="Gray"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>
        <Style x:Key="BodyCell" TargetType="Border">
            <Setter Property="Padding" Value="10"/>
            <Setter Property="BorderBrush" Value="Gray"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>
    </Window.Resources>
    <Window.DataContext>
        <vm:VifVilefViewModel/>
    </Window.DataContext>
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Tabla principal -->
        <Grid Grid.Row="0" Margin="0,0,0,10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="360"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Encabezados -->
            <Border Grid.Row="0" Grid.Column="0" Style="{StaticResource HeaderCell}"><TextBlock Text="Tecnica empleada" FontWeight="Bold"/></Border>
            <Border Grid.Row="0" Grid.Column="1" Style="{StaticResource HeaderCell}"><TextBlock Text="Resultado" FontWeight="Bold"/></Border>

            <!-- Celdas cuerpo -->
            <Border Grid.Row="1" Grid.Column="0" Style="{StaticResource BodyCell}">
                <TextBlock FontWeight="Bold" TextWrapping="Wrap">
                    <Run Text="Inmunocromatografia (IC)"/>
                    <LineBreak/>
                    <Run Text="Speed Duo FeLV/FIV"/>
                </TextBlock>
            </Border>
            <Border Grid.Row="1" Grid.Column="1" Style="{StaticResource BodyCell}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="70"/>
                        <ColumnDefinition Width="220"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Row="0" Grid.Column="0" Text="VIF:" FontWeight="Bold" VerticalAlignment="Center"/>
                    <ComboBox Grid.Row="0" Grid.Column="1" ItemsSource="{Binding ResultadosPosibles}" SelectedItem="{Binding VifResultado, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="160"/>

                    <TextBlock Grid.Row="1" Grid.Column="0" Text="VILeF:" FontWeight="Bold" VerticalAlignment="Center" Margin="0,6,0,0"/>
                    <ComboBox Grid.Row="1" Grid.Column="1" ItemsSource="{Binding ResultadosPosibles}" SelectedItem="{Binding VilefResultado, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="160" Margin="0,6,0,0"/>
                </Grid>
            </Border>
        </Grid>

        <StackPanel Grid.Row="2" Orientation="Vertical" Margin="0,0,0,0">
            <TextBlock Text="Observaciones" FontWeight="Bold"/>
            <TextBox Text="{Binding Observaciones, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" AcceptsReturn="True" Height="140" TextWrapping="Wrap"/>
        </StackPanel>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,12,0,0">
            <Button Content="Confirmar" Width="160" Height="36" Click="OnConfirmClick" Style="{StaticResource PrimaryButton}"/>
            <Button Content="Limpiar" Width="140" Height="36" Click="OnClearClick" Style="{StaticResource DangerButton}"/>
        </StackPanel>
    </Grid>
</Window>